import{useEffect,useState}from"react";import{financeRepository}from"./repository";import type{FinanceData}from"./types";export function useFinance(){const[data,setData]=useState<FinanceData>({transactions:[],subscriptions:[],goals:[],name:"",notificationsEnabled:true});useEffect(()=>setData(financeRepository.get()),[]);useEffect(()=>{financeRepository.save(data)},[data]);return[data,setData]as const}
